class Object {
    constructor(mesh) {
        this.mesh = mesh;
        this.transform = new Transform();
        //@todo:add a transform for this mesh which will contain it's transformation matrix
    }
}